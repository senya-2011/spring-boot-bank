/**
 * OpenAPI definition
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The CardResponse model module.
 * @module model/CardResponse
 * @version v0
 */
class CardResponse {
    /**
     * Constructs a new <code>CardResponse</code>.
     * @alias module:model/CardResponse
     */
    constructor() { 
        
        CardResponse.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>CardResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CardResponse} obj Optional instance to populate.
     * @return {module:model/CardResponse} The populated <code>CardResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CardResponse();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('maskedCardNumber')) {
                obj['maskedCardNumber'] = ApiClient.convertToType(data['maskedCardNumber'], 'String');
            }
            if (data.hasOwnProperty('ownerName')) {
                obj['ownerName'] = ApiClient.convertToType(data['ownerName'], 'String');
            }
            if (data.hasOwnProperty('expiry')) {
                obj['expiry'] = ApiClient.convertToType(data['expiry'], 'Date');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
            if (data.hasOwnProperty('balanceMinor')) {
                obj['balanceMinor'] = ApiClient.convertToType(data['balanceMinor'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CardResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CardResponse</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['maskedCardNumber'] && !(typeof data['maskedCardNumber'] === 'string' || data['maskedCardNumber'] instanceof String)) {
            throw new Error("Expected the field `maskedCardNumber` to be a primitive type in the JSON string but got " + data['maskedCardNumber']);
        }
        // ensure the json data is a string
        if (data['ownerName'] && !(typeof data['ownerName'] === 'string' || data['ownerName'] instanceof String)) {
            throw new Error("Expected the field `ownerName` to be a primitive type in the JSON string but got " + data['ownerName']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }

        return true;
    }


}



/**
 * @member {Number} id
 */
CardResponse.prototype['id'] = undefined;

/**
 * @member {String} maskedCardNumber
 */
CardResponse.prototype['maskedCardNumber'] = undefined;

/**
 * @member {String} ownerName
 */
CardResponse.prototype['ownerName'] = undefined;

/**
 * @member {Date} expiry
 */
CardResponse.prototype['expiry'] = undefined;

/**
 * @member {module:model/CardResponse.StatusEnum} status
 */
CardResponse.prototype['status'] = undefined;

/**
 * @member {Number} balanceMinor
 */
CardResponse.prototype['balanceMinor'] = undefined;





/**
 * Allowed values for the <code>status</code> property.
 * @enum {String}
 * @readonly
 */
CardResponse['StatusEnum'] = {

    /**
     * value: "ACTIVE"
     * @const
     */
    "ACTIVE": "ACTIVE",

    /**
     * value: "BLOCKED"
     * @const
     */
    "BLOCKED": "BLOCKED",

    /**
     * value: "EXPIRED"
     * @const
     */
    "EXPIRED": "EXPIRED"
};



export default CardResponse;

